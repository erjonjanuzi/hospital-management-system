export interface Department {
    id: string;
    name: string;
    capacity: string;
    description: string;
}

export interface DepartmentTable{
    name:string;
    capacity:string;
   description:string;
}
